num random = randomInt(9);
if (random == 0) block(0, 0, 0, "minecraft:suspicious_gravel");
if (random > 0 &&  random < 4) block(0, 0, 0, "minecraft:suspicious_gravel");
if (random < 8) block(0, 0, 0, "minecraft:suspicious_gravel");
